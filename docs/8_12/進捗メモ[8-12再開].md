ここまでの流れをまとめます。

① 設計フェーズ
決済システムをより本物のECサイトに近づけるため、「Amazon(EC)・三井住友(決済)・Dell(商品)」という三すくみ構造で分散システムを作ることにしました。通信方式(WebClient/HTTP)、状態管理(Sagaパターン、PENDING→STOCK_RESERVED→COMPLETED等の状態遷移図)、トークン方式(生カード番号をAmazon側に持たせない)、相関ID(ORD-yyyyMMdd-連番4桁)を1つずつ議論して固めました。

② 実装フェーズ(3プロジェクト構築)

dell-inventory(port 8081): 在庫の仮確保・確定・解放API → 動作確認済み
smbc-payment(port 8082): トークン発行・決済API → 動作確認済み
amazon-ec(port 8080): 上記2社をWebClientで呼び出し、Sagaを実行する統合処理(/orders) → 4パターン全て動作確認済み
シナリオ	結果
正常系	COMPLETED
決済失敗+補償成功	CANCELLED
在庫不足	STOCK_FAILED
決済失敗+補償失敗	COMPENSATION_FAILED

③ 管理者画面
Thymeleafで/admin/ordersを実装。全注文をステータス別に色分け表示し、フィルタリングもできるようにしました。

④ ユーザー画面(今ここ)
あなたから「他のユーザーのデータが見えてしまうのはリアルさに欠ける」という指摘があり、本格的なSpring Security認証を導入することにしました。

Userエンティティ(smbcTokenフィールドでカード登録済みトークンを保持し、注文のたびにカード入力しなくて済むようにする設計)
Flyway V2マイグレーション(usersテーブル)
サインアップ・ログイン機能(BCryptでパスワードハッシュ化)

ここまで実装・動作確認済みです。ログイン後の遷移先(/products)がまだ存在しないため、404エラーが出ている状態でした。

次にやる予定だったこと

カード登録画面(SMBCの/payment/tokensを叩き、返ってきたトークンをUser.smbcTokenに保存)
商品一覧+購入画面
注文履歴画面(ログイン中の本人の注文だけ表示)

このまま③(カード登録画面)から再開しますか?